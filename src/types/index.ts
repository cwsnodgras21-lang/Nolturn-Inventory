/** Shared product-level types. Domain types live under src/modules/*. */
export type PhaseStatus = "planned" | "in_progress" | "available";

export type OrganizationId = string;
export type UserId = string;
