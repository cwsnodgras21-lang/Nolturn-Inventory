-- Seed reference notes for `supabase db reset`.
-- Auth users cannot be created safely from SQL alone.
-- After reset, run: npm run db:bootstrap

-- Optional sample audit placeholder (no-op if no orgs yet).
select 1;
