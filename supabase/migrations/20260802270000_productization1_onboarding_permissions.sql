-- Productization 1: onboarding reuses existing permission keys.
-- organization.manage / settings.manage — branding + onboarding steps
-- members.manage / roles.manage — invitations and role assignment
-- locations.manage — primary location setup
-- catalog.manage — starter packs + CSV item import
-- inventory.storage.manage — optional starter storage structure
-- No new permission rows required for this phase.

select 1;
